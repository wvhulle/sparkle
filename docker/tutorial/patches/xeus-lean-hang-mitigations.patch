diff --git a/src/Display.lean b/src/Display.lean
index 2fd3371..8c28325 100644
--- a/src/Display.lean
+++ b/src/Display.lean
@@ -1203,6 +1203,11 @@ def waveformJSHtml (sessionId : String) (laneNames : List String)
     "    //     visible-tab call retries the same key.",
     "    if (typeof document !== 'undefined' && document.visibilityState",
     "        && document.visibilityState !== 'visible') return;",
+    "    // (b') JupyterLab in-page tab swap: even when the browser tab",
+    "    //      is visible, an inactive JupyterLab tab has its root",
+    "    //      hidden from layout (`root.offsetParent === null`).  Don't",
+    "    //      send queries for a widget the user isn't looking at.",
+    "    if (root && !root.offsetParent) return;",
     "    // (a) rate-limit drag-induced queries.  A genuine viewport",
     "    //     change still gets through after the gap; the cache",
     "    //     prevents redundant ones.",
@@ -1395,14 +1400,31 @@ def waveformJSHtml (sessionId : String) (laneNames : List String)
     "  }",
     "",
     "  // --- Animation loop --------------------------------------------------",
+    "  // Visibility gate.  `document.hidden` only flips when the browser",
+    "  // tab itself is hidden — *not* when JupyterLab switches between",
+    "  // its in-page notebook tabs.  In JupyterLab the previous tab's",
+    "  // root <div> just gets its parent hidden, so `root.offsetParent`",
+    "  // becomes null while `document.hidden` stays false.  We check both:",
+    "  // if either says we're not visible, suspend RAF entirely so the",
+    "  // browser main thread can serve whatever tab the user is actually",
+    "  // looking at (e.g. opening ch04 while ch03's wdb widget is in the",
+    "  // background).  We re-arm on visibilitychange (browser-tab) and on",
+    "  // a MutationObserver below (in-page tab).",
+    "  function isVisible() {",
+    "    if (typeof document !== 'undefined' && document.hidden) return false;",
+    "    if (!root || !root.isConnected) return false;",
+    "    return root.offsetParent !== null;",
+    "  }",
     "  let rafPending = false;",
     "  function kickAnimation() {",
     "    if (rafPending) return;",
+    "    if (!isVisible()) return;",
     "    rafPending = true;",
     "    requestAnimationFrame(animFrame);",
     "  }",
     "  function animFrame() {",
     "    rafPending = false;",
+    "    if (!isVisible()) return;",
     "    // Lerp cur → target with a fixed coefficient. Visually, motion",
     "    // settles in ~10 frames (≈ 170 ms at 60 Hz).",
     "    const r = 0.25;",
@@ -1418,6 +1440,22 @@ def waveformJSHtml (sessionId : String) (laneNames : List String)
     "    drawFrameAll();",
     "    if (moving) kickAnimation();",
     "  }",
+    "  // Watch for our root becoming visible/hidden via JupyterLab's",
+    "  // in-page tab swap.  When the user switches *to* this tab we",
+    "  // re-render once to catch any state that drifted while hidden.",
+    "  if (typeof MutationObserver !== 'undefined' && root.parentElement) {",
+    "    let wasVisible = isVisible();",
+    "    const obs = new MutationObserver(() => {",
+    "      const nowVis = isVisible();",
+    "      if (nowVis && !wasVisible) {",
+    "        wasVisible = true;",
+    "        kickAnimation();",
+    "      } else if (!nowVis) {",
+    "        wasVisible = false;",
+    "      }",
+    "    });",
+    "    obs.observe(document.body, { attributes: true, childList: true, subtree: true });",
+    "  }",
     "",
     "  function setTarget(t0, span) {",
     "    const c = clampViewport(t0, span);",
